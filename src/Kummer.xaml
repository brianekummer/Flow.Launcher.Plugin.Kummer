<UserControl x:Class="Flow.Launcher.Plugin.Kummer.KummerSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Path=Settings, RelativeSource={RelativeSource Self}}">
    <Grid Margin="10" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="43*" />
            <RowDefinition Height="43*" />
            <RowDefinition Height="43*" />
            <RowDefinition Height="37*" />
            <RowDefinition Height="23*" />
            <RowDefinition Height="31*" />
            <RowDefinition Height="31*" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Slack Tokens" Margin="0,10,0,6"></Label>
        <TextBox Grid.Row="0" Text="{Binding SlackTokens}" Margin="120,11,0,9" HorizontalAlignment="Left" Width="357" />
        <Label Grid.Row="3" Content="You need to restart Flow Launcher to apply the changes." FontWeight="Bold">
        </Label>

    </Grid>
</UserControl>
